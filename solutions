<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Business Capabilities Infographic</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <!-- Chosen Palette: Energetic & Playful -->
    <!-- Application Structure Plan: The infographic is designed with a top-down narrative flow. It starts with high-impact summary statistics ("big numbers") to give an immediate sense of the framework's scale. This is followed by a central doughnut chart that visualizes the distribution of capabilities across the main domains. The body of the page uses a responsive grid of cards, each dedicated to a primary domain, allowing users to explore the details. This structure guides the user from a broad overview to specific details, making the complex hierarchy easy to understand. -->
    <!-- Visualization & Content Choices: 
        - Key Metrics (Domains, Areas, etc.): Goal: Inform. Method: Large, bold text. Justification: These "big number" callouts act as an effective hook, quickly conveying the scope and scale of the data. NO SVG is used.
        - Domain Distribution: Goal: Compare Composition. Viz: Doughnut Chart (Chart.js/Canvas). Justification: This is ideal for showing parts of a whole, clearly illustrating the relative size of each primary domain. It serves as a visual centerpiece. Tooltip and label wrapping are included for clarity. NO SVG is used.
        - Hierarchical Breakdown: Goal: Organize. Method: Styled HTML cards with nested lists (Tailwind). Justification: For this type of nested textual data, a clean card layout with lists is far more readable and scannable than a complex diagram, especially on various screen sizes. Unicode characters are used for notes. NO SVG/Mermaid is used. -->
    <!-- CONFIRMATION: NO SVG graphics used. NO Mermaid JS used. -->
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8fafc;
        }
        .gradient-text {
            background: linear-gradient(90deg, #7a5195, #ef5675);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .card {
            background-color: #ffffff;
            border-radius: 0.75rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
            border: 1px solid #e5e7eb;
            display: flex;
            flex-direction: column;
        }
        .card-content {
            flex-grow: 1;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -4px rgba(0, 0, 0, 0.1);
        }
        .chart-container {
            position: relative;
            width: 100%;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
            height: 45vh;
            max-height: 500px;
        }
    </style>
</head>
<body class="text-gray-800">

    <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-16">

        <header class="text-center mb-16">
            <h1 class="text-4xl md:text-5xl font-extrabold tracking-tight">
                <span class="gradient-text">Business Capabilities Framework</span>
            </h1>
            <p class="mt-4 text-lg text-gray-600 max-w-3xl mx-auto">A visual exploration of the core capabilities driving customer security, product integrity, and compliance.</p>
        </header>

        <section class="mb-20">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
                <div class="bg-white p-8 rounded-xl shadow-lg">
                    <p class="text-5xl font-bold text-[#7a5195]">4</p>
                    <p class="text-lg font-medium text-gray-700 mt-2">Primary Domains</p>
                </div>
                <div class="bg-white p-8 rounded-xl shadow-lg">
                    <p class="text-5xl font-bold text-[#ef5675]">15+</p>
                    <p class="text-lg font-medium text-gray-700 mt-2">Key Functional Areas</p>
                </div>
                <div class="bg-white p-8 rounded-xl shadow-lg">
                    <p class="text-5xl font-bold text-[#ffa600]">20+</p>
                    <p class="text-lg font-medium text-gray-700 mt-2">Total Capabilities</p>
                </div>
            </div>
        </section>

        <section class="bg-white rounded-xl shadow-lg p-6 md:p-8 mb-20">
            <h2 class="text-2xl md:text-3xl font-bold text-center mb-2">Distribution of Capabilities by Domain</h2>
            <p class="text-center text-gray-600 mb-8">This chart illustrates the proportion of key functional areas within each primary domain.</p>
            <div class="chart-container">
                <canvas id="capabilitiesChart"></canvas>
            </div>
        </section>

        <section>
            <h2 class="text-2xl md:text-3xl font-bold text-center mb-12">Detailed Domain Breakdown</h2>
            <div id="capabilities-grid" class="grid grid-cols-1 md:grid-cols-2 gap-8">
            </div>
        </section>

    </div>

    <script>
        const infographicData = {
            "Customer Account Security": {
                "Sign-up protection": null,
                "Sign-in protection": { "📝 Note": "IRIS and IRIS decomposition in future" },
                "Customer Interaction Security": null,
                "High Risk Interactions": null,
                "Customer Security Portal": null,
                "Customer account security intelligence": null
            },
            "Product Abuse": {
                "Abuse Signal Gathering": null,
                "Product Abuse Mitigation By Design": null,
                "Third Party Developer Ecosystem Abuse": null,
                "Campaign Safety": {
                    "Campaign Content Safety": null,
                    "Campaign Abuse and Risk Prevention": null,
                    "Legacy MC": null
                },
                "Customer correspondes content safety": null,
                "AI Interaction Content Safety": {
                    "Customer to AI": null,
                    "AI to AI": null
                },
                "Ecosystem abuse prevention": { "📝 Note": "sms pumping, Add FI, wallet" },
                "Product abuse intelligence": null
            },
            "Customer verification": {
                "Negative Entity Continuous Screening": null,
                "Regulatory Screening": {
                    "OFAC Screening": null,
                    "PEP Screening": null,
                    "FinCEN screening": null,
                    "Regulatory screening intelligence": null
                }
            },
            "Tax Fraud": {
                "Tax Filing Fraud": null,
                "Trusted Auth": null,
                "Tax Fraud intelligence": null
            }
        };

        function createNestedList(data) {
            let html = '<ul class="list-disc list-inside text-gray-600 mt-1 pl-2 space-y-1">';
            for (const key in data) {
                html += `<li>${key}`;
                if (typeof data[key] === 'object' && data[key] !== null) {
                    html += createNestedList(data[key]);
                } else if (data[key] !== null) {
                    html += `<span class="text-xs text-gray-500 ml-2">${data[key]}</span>`;
                }
                if (key.startsWith("📝 Note")) {
                    html = html.replace(`<li>${key}`, `<li class="list-none text-xs bg-blue-50 border border-blue-200 text-blue-800 rounded p-2 mt-1">${key.replace(/📝\s*/, '')}`);
                }
                html += '</li>';
            }
            html += '</ul>';
            return html;
        }

        document.addEventListener('DOMContentLoaded', () => {
            const grid = document.getElementById('capabilities-grid');
            const chartLabels = Object.keys(infographicData);
            const chartData = chartLabels.map(key => Object.keys(infographicData[key]).length);
            const chartColors = ['#003f5c', '#7a5195', '#ef5675', '#ffa600'];
            
            let colorIndex = 0;
            for (const h1 in infographicData) {
                let cardHtml = `<div class="card p-6">
                                <h3 class="text-xl font-bold mb-4" style="color: ${chartColors[colorIndex % chartColors.length]};">${h1}</h3>
                                <div class="card-content">`;
                cardHtml += createNestedList(infographicData[h1]);
                cardHtml += `</div></div>`;
                grid.innerHTML += cardHtml;
                colorIndex++;
            }

            const processLabel = (label) => {
                if (label.length > 16) {
                    const words = label.split(' ');
                    let lines = [];
                    let currentLine = '';
                    for (const word of words) {
                        if ((currentLine + ' ' + word).trim().length > 16 && currentLine.length > 0) {
                            lines.push(currentLine);
                            currentLine = word;
                        } else {
                            currentLine = currentLine ? currentLine + ' ' + word : word;
                        }
                    }
                    lines.push(currentLine);
                    return lines;
                }
                return label;
            };

            const processedLabels = chartLabels.map(processLabel);

            const ctx = document.getElementById('capabilitiesChart').getContext('2d');
            const capabilitiesChart = new Chart(ctx, {
                type: 'doughnut',
                data: {
                    labels: processedLabels,
                    datasets: [{
                        label: 'Functional Areas',
                        data: chartData,
                        backgroundColor: chartColors,
                        borderColor: '#ffffff',
                        borderWidth: 3,
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'bottom',
                            labels: {
                                font: { size: 12, family: "'Inter', sans-serif" },
                                boxWidth: 15, padding: 20
                            }
                        },
                        tooltip: {
                            enabled: true,
                            callbacks: {
                                title: function(tooltipItems) {
                                    const item = tooltipItems[0];
                                    let label = item.chart.data.labels[item.dataIndex];
                                    if (Array.isArray(label)) {
                                      return label.join(' ');
                                    }
                                    return label;
                                }
                            },
                            backgroundColor: '#333', titleFont: { size: 14 }, bodyFont: { size: 12 }, padding: 10, cornerRadius: 4
                        }
                    }
                }
            });
        });
    </script>
</body>
</html>
